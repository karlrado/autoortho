# Build requirements for AutoOrtho
#
# PyInstaller is used for building standalone executables.
# It bundles the Python interpreter and all dependencies.
#
# Install with: pip install -r requirements-build.txt

pyinstaller>=6.18.0
    # Primary build tool - creates standalone executables
    # Uses real CPython interpreter for maximum compatibility
